name: console-patterns
region: nyc
static_sites:
  - build_command: yarn build && yarn next export
    environment_slug: node-js
    github:
      branch: master
      deploy_on_push: true
      repo: grikomsn/console-patterns
    name: console-patterns
    output_dir: out
    routes:
      - path: /
